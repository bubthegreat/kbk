name: Deploy Branch to Environment

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to deploy (e.g., feature/my-feature, help-commands, etc.)'
        required: true
        type: string
        default: 'master'
      environment:
        description: 'Target environment'
        required: true
        type: choice
        options:
          - dev
          - staging
          - prod
      build_images:
        description: 'Build new Docker images?'
        required: true
        type: boolean
        default: false

env:
  DOCKER_REGISTRY: docker.io
  DOCKER_USERNAME: bubthegreat
  KBK_IMAGE_NAME: bubthegreat/kbk
  SQL_IMAGE_NAME: bubthegreat/kbk-sql

jobs:
  validate-branch:
    name: Validate Branch
    runs-on: ubuntu-latest
    outputs:
      branch_exists: ${{ steps.check.outputs.exists }}

    steps:
      - name: Checkout master
        uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0

      - name: List available branches
        run: |
          echo "### Available Branches" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          git branch -r | grep -v HEAD | sed 's/origin\///' | sort >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

      - name: Check if branch exists
        id: check
        run: |
          if git show-ref --verify --quiet refs/remotes/origin/${{ inputs.branch }}; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "✅ Branch '${{ inputs.branch }}' exists" >> $GITHUB_STEP_SUMMARY
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "❌ Branch '${{ inputs.branch }}' does not exist" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

  build-images:
    name: Build Docker Images
    runs-on: ubuntu-latest
    needs: validate-branch
    if: inputs.build_images == true && needs.validate-branch.outputs.branch_exists == 'true'
    outputs:
      short_sha: ${{ steps.vars.outputs.short_sha }}
      branch_name: ${{ steps.vars.outputs.branch_name }}
      mysql_changed: ${{ steps.mysql_changes.outputs.changed }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch }}
          fetch-depth: 0

      - name: Set up variables
        id: vars
        run: |
          echo "short_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

          # Sanitize branch name for use in tags
          SAFE_BRANCH_NAME=$(echo "${{ inputs.branch }}" | sed 's/\//-/g')
          echo "branch_name=${SAFE_BRANCH_NAME}" >> $GITHUB_OUTPUT

      - name: Check for MySQL changes
        id: mysql_changes
        run: |
          # Check if mysql directory has changed in this commit
          if git diff --name-only HEAD~1 HEAD | grep -q '^mysql/'; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "MySQL files changed - will rebuild SQL image"
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "No MySQL changes detected - skipping SQL image build"
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract metadata for KBK image
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.KBK_IMAGE_NAME }}
          tags: |
            type=sha,prefix=,format=short
            type=raw,value=${{ steps.vars.outputs.branch_name }}-${{ steps.vars.outputs.short_sha }}
            type=raw,value=${{ steps.vars.outputs.branch_name }}-latest

      - name: Build and push KBK image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=registry,ref=${{ env.KBK_IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.KBK_IMAGE_NAME }}:buildcache,mode=max

      - name: Extract metadata for SQL image
        id: meta-sql
        if: steps.mysql_changes.outputs.changed == 'true'
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.SQL_IMAGE_NAME }}
          tags: |
            type=sha,prefix=,format=short
            type=raw,value=${{ steps.vars.outputs.branch_name }}-${{ steps.vars.outputs.short_sha }}
            type=raw,value=${{ steps.vars.outputs.branch_name }}-latest

      - name: Build and push SQL image
        if: steps.mysql_changes.outputs.changed == 'true'
        uses: docker/build-push-action@v5
        with:
          context: ./mysql
          push: true
          tags: ${{ steps.meta-sql.outputs.tags }}
          cache-from: type=registry,ref=${{ env.SQL_IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.SQL_IMAGE_NAME }}:buildcache,mode=max

  get-sha:
    name: Get Branch SHA
    runs-on: ubuntu-latest
    needs: validate-branch
    if: inputs.build_images == false && needs.validate-branch.outputs.branch_exists == 'true'
    outputs:
      short_sha: ${{ steps.vars.outputs.short_sha }}
      branch_name: ${{ steps.vars.outputs.branch_name }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch }}
          fetch-depth: 0

      - name: Set up variables
        id: vars
        run: |
          echo "short_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          SAFE_BRANCH_NAME=$(echo "${{ inputs.branch }}" | sed 's/\//-/g')
          echo "branch_name=${SAFE_BRANCH_NAME}" >> $GITHUB_OUTPUT

  update-manifests:
    name: Update Manifests for ${{ inputs.environment }}
    runs-on: ubuntu-latest
    needs: [validate-branch, build-images, get-sha]
    if: always() && needs.validate-branch.outputs.branch_exists == 'true' && (needs.build-images.result == 'success' || needs.get-sha.result == 'success')
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set variables
        id: vars
        run: |
          if [ "${{ inputs.build_images }}" == "true" ]; then
            echo "short_sha=${{ needs.build-images.outputs.short_sha }}" >> $GITHUB_OUTPUT
            echo "branch_name=${{ needs.build-images.outputs.branch_name }}" >> $GITHUB_OUTPUT
          else
            echo "short_sha=${{ needs.get-sha.outputs.short_sha }}" >> $GITHUB_OUTPUT
            echo "branch_name=${{ needs.get-sha.outputs.branch_name }}" >> $GITHUB_OUTPUT
          fi

      - name: Update manifests for production
        if: inputs.environment == 'prod'
        run: |
          SHORT_SHA="${{ steps.vars.outputs.short_sha }}"
          BRANCH_NAME="${{ steps.vars.outputs.branch_name }}"
          MYSQL_CHANGED="${{ needs.build-images.outputs.mysql_changed }}"

          # Update KBK deployment
          sed -i "s|image: bubthegreat/kbk:.*|image: bubthegreat/kbk:${BRANCH_NAME}-${SHORT_SHA}|g" k8s/base/kbk-deployment.yaml

          # Only update MySQL deployment if MySQL files changed
          if [ "$MYSQL_CHANGED" == "true" ]; then
            sed -i "s|image: bubthegreat/kbk-sql:.*|image: bubthegreat/kbk-sql:${BRANCH_NAME}-${SHORT_SHA}|g" k8s/base/mysql-deployment.yaml
            echo "Updated production base manifests (KBK + MySQL) to ${BRANCH_NAME}-${SHORT_SHA}"
          else
            echo "Updated production base manifests (KBK only) to ${BRANCH_NAME}-${SHORT_SHA}"
            echo "MySQL manifest not updated (no MySQL changes detected)"
          fi

      - name: Update manifests for staging
        if: inputs.environment == 'staging'
        run: |
          SHORT_SHA="${{ steps.vars.outputs.short_sha }}"
          BRANCH_NAME="${{ steps.vars.outputs.branch_name }}"
          MYSQL_CHANGED="${{ needs.build-images.outputs.mysql_changed }}"

          # Create/update staging overlay
          mkdir -p k8s/overlays/kbk-staging

          # Build images section conditionally
          IMAGES_SECTION="images:
            - name: bubthegreat/kbk
              newTag: ${BRANCH_NAME}-${SHORT_SHA}"

          if [ "$MYSQL_CHANGED" == "true" ]; then
            IMAGES_SECTION="${IMAGES_SECTION}
            - name: bubthegreat/kbk-sql
              newTag: ${BRANCH_NAME}-${SHORT_SHA}"
          fi

          cat > k8s/overlays/kbk-staging/kustomization.yaml << EOF
          apiVersion: kustomize.config.k8s.io/v1beta1
          kind: Kustomization

          namespace: kbk-staging

          resources:
            - namespace.yaml
            - ../../base

          commonLabels:
            environment: staging

          commonAnnotations:
            deployed-branch: ${BRANCH_NAME}
            deployed-sha: ${SHORT_SHA}

          patchesStrategicMerge:
            - ingress-patch.yaml

          ${IMAGES_SECTION}
          EOF

          if [ "$MYSQL_CHANGED" == "true" ]; then
            echo "Updated staging overlay (KBK + MySQL) to ${BRANCH_NAME}-${SHORT_SHA}"
          else
            echo "Updated staging overlay (KBK only) to ${BRANCH_NAME}-${SHORT_SHA}"
          fi

      - name: Update manifests for dev
        if: inputs.environment == 'dev'
        run: |
          SHORT_SHA="${{ steps.vars.outputs.short_sha }}"
          BRANCH_NAME="${{ steps.vars.outputs.branch_name }}"
          MYSQL_CHANGED="${{ needs.build-images.outputs.mysql_changed }}"

          # Create/update dev overlay
          mkdir -p k8s/overlays/dev

          # Build images section conditionally
          IMAGES_SECTION="images:
            - name: bubthegreat/kbk
              newTag: ${BRANCH_NAME}-${SHORT_SHA}"

          if [ "$MYSQL_CHANGED" == "true" ]; then
            IMAGES_SECTION="${IMAGES_SECTION}
            - name: bubthegreat/kbk-sql
              newTag: ${BRANCH_NAME}-${SHORT_SHA}"
          fi

          cat > k8s/overlays/dev/kustomization.yaml << EOF
          apiVersion: kustomize.config.k8s.io/v1beta1
          kind: Kustomization

          namespace: kbk-dev

          resources:
            - namespace.yaml
            - ../../base

          commonLabels:
            environment: dev

          commonAnnotations:
            deployed-branch: ${BRANCH_NAME}
            deployed-sha: ${SHORT_SHA}

          patchesStrategicMerge:
            - ingress-patch.yaml

          ${IMAGES_SECTION}
          EOF

          if [ "$MYSQL_CHANGED" == "true" ]; then
            echo "Updated dev overlay (KBK + MySQL) to ${BRANCH_NAME}-${SHORT_SHA}"
          else
            echo "Updated dev overlay (KBK only) to ${BRANCH_NAME}-${SHORT_SHA}"
          fi

      - name: Commit and push manifest changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          BRANCH_NAME="${{ steps.vars.outputs.branch_name }}"
          SHORT_SHA="${{ steps.vars.outputs.short_sha }}"
          ENV="${{ inputs.environment }}"

          if git diff --quiet; then
            echo "No changes to commit"
          else
            git add k8s/
            git commit -m "chore: deploy branch ${{ inputs.branch }} (${BRANCH_NAME}-${SHORT_SHA}) to ${ENV} [skip ci]"
            git push
          fi

      - name: Create deployment summary
        run: |
          BRANCH_NAME="${{ steps.vars.outputs.branch_name }}"
          SHORT_SHA="${{ steps.vars.outputs.short_sha }}"
          ENV="${{ inputs.environment }}"
          MYSQL_CHANGED="${{ needs.build-images.outputs.mysql_changed }}"

          cat >> $GITHUB_STEP_SUMMARY << EOF
          ## 🚀 Manual Deployment Summary

          ### Configuration
          - **Source Branch**: \`${{ inputs.branch }}\`
          - **Target Environment**: \`${ENV}\`
          - **Built New Images**: \`${{ inputs.build_images }}\`

          ### Images Deployed
          - **KBK Image**: \`bubthegreat/kbk:${BRANCH_NAME}-${SHORT_SHA}\`
          EOF

          if [ "$MYSQL_CHANGED" == "true" ]; then
            echo "- **SQL Image**: \`bubthegreat/kbk-sql:${BRANCH_NAME}-${SHORT_SHA}\` ✅ **Updated**" >> $GITHUB_STEP_SUMMARY
          else
            echo "- **SQL Image**: ⏭️ **Skipped** (no MySQL changes detected)" >> $GITHUB_STEP_SUMMARY
          fi

          cat >> $GITHUB_STEP_SUMMARY << EOF

          ### Manifests Updated
          EOF

          if [ "${ENV}" == "prod" ]; then
            echo "- \`k8s/base/kbk-deployment.yaml\`" >> $GITHUB_STEP_SUMMARY
            if [ "$MYSQL_CHANGED" == "true" ]; then
              echo "- \`k8s/base/mysql-deployment.yaml\`" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "- \`k8s/overlays/${ENV}/kustomization.yaml\`" >> $GITHUB_STEP_SUMMARY
          fi

          cat >> $GITHUB_STEP_SUMMARY << EOF

          ### Next Steps
          ArgoCD will automatically sync the new images to **kbk-${ENV}** namespace within ~3 minutes.

          Monitor deployment:
          \`\`\`bash
          argocd app get kbk-${ENV}
          kubectl get pods -n kbk-${ENV} -w
          \`\`\`

          ### Rollback
          To rollback, run this workflow again with the previous branch/commit.
          EOF

