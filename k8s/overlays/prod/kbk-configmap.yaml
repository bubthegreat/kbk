apiVersion: v1
kind: ConfigMap
metadata:
  name: kbk-config
data:
  # GDB debugging is intentionally ENABLED in production: crashes are rare
  # enough that live backtraces are worth the performance cost. Probe
  # timeouts in the base deployment are tuned to tolerate gdb's overhead.
  ENABLE_GDB_DEBUGGING: "true"

