apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kbk-ingress-nginx-tcp-config
  namespace: argocd
spec:
  project: default
  
  source:
    repoURL: https://github.com/bubthegreat/kbk.git
    targetRevision: HEAD
    path: k8s/ingress-nginx
  
  destination:
    server: https://kubernetes.default.svc
    namespace: ingress-nginx
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

